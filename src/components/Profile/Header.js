import React, { useState, useEffect } from 'react';

export default function Header({ 
  photosCount,
  followerCount,
  setFollowerCount,
  profile,
  username
}) {
  return <p>I am the header of the profile</p>;
}